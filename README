To run this app, first be sure node.js and npm are installed. 

Then run '[sudo] npm install' for the packages. With luck I have not missed anything. 

To start the app, run 'node app.js' or 'npm start'.

It should then be accessible on localhost:3000 in your browser. 
